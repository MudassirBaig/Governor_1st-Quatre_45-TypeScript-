// class BMICalculator {
//     private weight: number;
//     private height: number;
//     constructor(weight: number, height: number) {
//         this.weight = weight;
//         this.height = height;
//     }
//     calculateBMI(): number {
//         // BMI formula: weight (kg) / (height (m) * height (m))
//         return this.weight / ((this.height / 100) ** 2);
//     }
//     add(value: number): number {
//         return this.weight + value;
//     }
//     subtract(value: number): number {
//         return this.weight - value;
//     }
//     multiply(value: number): number {
//         return this.weight * value;
//     }
//     divide(value: number): number {
//         return this.weight / value;
//     }
//     exponential(value: number): number {
//         return Math.pow(this.weight, value);
//     }
//     modulus(value: number): number {
//         return this.weight % value;
//     }
// }
// // Example usage
// const weight = 70; // in kilograms
// const height = 170; // in centimeters
// const bmiCalculator = new BMICalculator(weight, height);
// const bmi = bmiCalculator.calculateBMI();
// console.log(`BMI: ${bmi.toFixed(2)}`);
// // Additional arithmetic operations
// console.log(`Weight + 5: ${bmiCalculator.add(5)}`);
// console.log(`Weight - 5: ${bmiCalculator.subtract(5)}`);
// console.log(`Weight * 2: ${bmiCalculator.multiply(2)}`);
// console.log(`Weight / 2: ${bmiCalculator.divide(2)}`);
// console.log(`Weight^3: ${bmiCalculator.exponential(3)}`);
// console.log(`Weight % 3: ${bmiCalculator.modulus(3)}`);
lett;
message = "Hello World"; //syntax error
console.log(message);
